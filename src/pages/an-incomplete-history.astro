---
import Base from '../layouts/Base.astro';
import { historyEvents } from '../data/history.js';

const sortedEvents = [...historyEvents].sort((a, b) => b.year - a.year);
const startYear = 1995;
const endYear = 2026;
---

<Base title="shea on air: an incomplete history" background="bg5">
  <p class="history-intro">
    Every moment is a culmination of every moment
  </p>

  <ol class="timeline" aria-label="Life timeline, newest first">
    {sortedEvents.map((event) => (
      <li class="timeline-entry">
        <time class="timeline-year" datetime={String(event.year)}>{event.year}</time>
        <div class="timeline-body">
          <p class="timeline-title">{event.title}</p>
        </div>
      </li>
    ))}
  </ol>
</Base>

<style>
  .history-intro {
    margin-bottom: 2rem;
    font-style: italic;
    margin-b
  }

  .timeline {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    border-left: 1px solid var(--color-border, currentColor);
    padding-left: var(--space-6, 1.5rem);
    max-width: 32rem;
    margin-left: 5rem;
  }

  .timeline-entry {
    display: grid;
    grid-template-columns: 4rem 1fr;
    gap: var(--space-4, 1rem);
    align-items: start;
  }

  .timeline-year {
    font-variant-numeric: tabular-nums;
    font-weight: 600;
    margin-left: 1rem;
    color: var(--color-muted);
    font-size: .875rem;
  }

  .timeline-title {
    margin: 0;
    padding-bottom: .5rem;
    font-size: 1rem;
  }

  .timeline-description {
    margin: 0;
    font-size: .75rem;
  }

  @media (max-width: 500px) {
    .timeline {
      margin-left: 2rem;
    }
  }
</style>
